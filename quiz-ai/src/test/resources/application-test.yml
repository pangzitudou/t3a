# AI服务测试环境配置

server:
  port: 0

spring:
  application:
    name: quiz-ai-test

  data:
    redis:
      host: localhost
      port: 6379

# Spring AI (测试环境使用mock)
#spring.ai.dashscope:
#  api-key: test-api-key
#  chat:
#    options:
#      model: qwen-max
#      temperature: 0.7
  ai:
    deepseek:
      api-key: ${DEEPSEEK_API_KEY}

# 日志
logging:
  level:
    com.t3a.ai: DEBUG
